# 公告系统设计改进计划

本文记录对 [HMCL-dev/HMCL#4544](https://github.com/HMCL-dev/HMCL/issues/4544) 中公告系统设计草案的改进计划。

## 目标

- 在启动器内支持远程公告拉取、本地缓存、弹窗公告和主页公告。
- 公告系统失败时不影响启动器启动和核心功能。
- 公告数据格式具备长期兼容性，便于后续扩展精准投放、安全渲染和过期清理。

## 服务端公告 Schema

- 将 `title` 统一为本地化文本对象，而不是单一字符串。
- 将正文和外部链接统一到单一 `content` 字段，使用 `type` 区分 `html` 与 `link`，并用 `value` 存储本地化内容。
- 固定 `type` 或展示目标的枚举值。
  - 如果一个公告可能同时用于主页和弹窗，考虑使用 `targets` 数组代替单一 `type`。
- 固定 `severity` 枚举，例如 `info`、`warning`、`critical`。
- 增加公告生命周期字段：
  - `startsAt`
  - `expiresAt`
- 复用 `CompatibilityRule` 描述公告的环境限制，避免单独定义 `platforms`、`channels`、`minVersion`、`maxVersion` 等字段。
- 明确 `parent` 的语义，避免“父公告未展示”因关闭、过期、语言不匹配或配置禁用而产生歧义。
- 明确排序规则：
  1. 严重性更高的公告优先。
  2. `priority` 更高的公告优先。
  3. 发布时间或生效时间更新的公告优先。
  4. `id` 作为稳定排序兜底。

## 本地缓存 Schema

- 将 `lastAccessTime` 改为更准确的 `lastFetchAttemptTime`。
- 增加 `lastSuccessfulFetchTime`，区分尝试请求和成功更新。
- 保存服务端返回的 `Last-Modified` 时间，优先使用 `If-Modified-Since` 进行条件请求。
- 本地缓存结构建议包含：
  - `lastFetchAttemptTime`
  - `lastSuccessfulFetchTime`
  - `lastModifiedTime`
  - `closed`
  - `announcements`
- 清理 `closed` 时只保留仍存在且仍有效的公告 ID。

## 拉取与失败策略

- 启动后异步刷新公告，不阻塞启动器主流程。
- 如果本地没有缓存，尝试从服务器拉取公告。
- 如果本地已有缓存且未超过刷新间隔，直接使用缓存。
- 如果超过刷新间隔，使用 `If-Modified-Since` 发起条件请求。
- 服务器返回 `304 Not Modified` 时只刷新 `lastFetchAttemptTime`。
- 服务器返回新公告时更新 `announcements`、`lastModifiedTime` 和成功刷新时间。
- 网络失败、解析失败或服务端返回异常格式时：
  - 保留旧缓存。
  - 不覆盖本地有效数据。
  - 不显示错误弹窗。
  - 记录日志便于排查。
- 如果无缓存且拉取失败，则不显示公告区域。

## 用户设置

- 增加公告功能开关，默认启用。
- 用户关闭公告功能后：
  - 不发起远程公告请求。
  - 不显示弹窗公告。
  - 不显示主页公告区域。
- 明确该设置是否允许被紧急公告绕过；默认不建议绕过用户设置。

## 展示与关闭规则

- 区分公告的展示行为：
  - `dismissible`：是否允许关闭。
  - `showOnce`：是否确认后不再显示。
  - `ackRequired`：是否要求用户确认。
- `popup` 公告不应简单等同于确认后永久关闭，需要由字段控制。
- `board` 公告关闭后加入 `closed`。
- 进入主界面后先过滤、再排序、最后渲染公告。
- 公告系统 UI 不应在离线、禁用或无可展示公告时占用主页空间。

## 内容安全

- 公告正文如果支持 HTML，必须使用白名单策略。
- 明确允许的标签、属性和 URL 协议。
- 禁止 `javascript:`、危险 `data:` URL、内联脚本和不受控远程资源。
- 链接点击应交给外部浏览器处理。
- 如果实现成本可控，优先考虑受限 Markdown 或受限富文本，而不是直接渲染任意 HTML。

## 安全验证

- 当前阶段可依赖 HTTPS 传输安全。
- 后续可评估是否需要公钥 pinning 或公告签名。
- 如果加入签名，应独立设计密钥轮换、回滚和兼容策略，避免阻塞第一版实现。

## 实施顺序

1. 定义公告服务端 schema 和本地缓存 schema。
2. 实现公告解析、校验、过滤和排序逻辑。
3. 实现本地缓存读写和 `If-Modified-Since` 条件请求。
4. 实现失败兜底策略，确保公告系统不会影响启动器启动。
5. 接入用户设置开关。
6. 实现主页 `board` 公告展示。
7. 实现启动时 `popup` 公告展示。
8. 补充 HTML 或富文本内容安全处理。
9. 添加针对缓存、过滤、排序、关闭状态和失败策略的测试。
